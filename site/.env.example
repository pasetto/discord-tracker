# Base do app Angular (CTA Criar conta → /signup, Entrar → /signin)
PUBLIC_APP_URL=http://localhost:4200

# Origem canônica do site marketing (sem path de project pages)
PUBLIC_SITE_URL=http://localhost:4321

# Base path Astro (`/` local; `/discord-tracker` no GitHub Pages)
PUBLIC_BASE_PATH=/
