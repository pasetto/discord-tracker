{
  "name": "syntra-site",
  "version": "0.1.0",
  "private": true,
  "description": "Landing marketing Syntra (estática, fora do Angular)",
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 0.0.0.0 --port 4321",
    "start": "astro dev --host 0.0.0.0 --port 4321",
    "build": "astro build",
    "preview": "astro preview --host 0.0.0.0 --port 4321",
    "check": "astro check"
  },
  "dependencies": {
    "astro": "^5.12.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
